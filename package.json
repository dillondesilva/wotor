{
  "name": "uno",
  "version": "0.0.1",
  "description": "My First HTML5 JavaScript Game",
  "author": "Dillon de Silva",
  "license": "MIT",
  "main": "out/index.js",
  "scripts": {
    "clean": "rm -rf out",
    "build": "npm run clean && babel src/ -d out/ --source-maps && cp -r game out/",
    "buildTests": "npm run clean && babel test/ -d out/test --source-maps && babel src/ -d out/src --source-maps",
    "eslint": "eslint --fix src test game/js",
    "test": "istanbul cover node_modules/.bin/_mocha -- -u exports --compilers js:babel-core/register -R spec -r test/setup.js test/*{,*/}*.js",
    "watch": "nodemon --ignore out/ --ignore node_modules/ --exec 'npm run eslint && npm run test && npm run build'",
    "server": "npm run build && nodemon --ignore node_modules/ --ignore src/ --ignore test/ out/index.js",
    "start": "npm run watch & sleep 3 && npm run server"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-eslint": "^7.1.1",
    "babel-plugin-transform-async-to-generator": "^6.22.0",
    "babel-plugin-transform-class-properties": "^6.23.0",
    "babel-polyfill": "^6.23.0",
    "babel-preset-es2015": "^6.22.0",
    "chai": "^3.5.0",
    "eslint": "^3.16.1",
    "eslint-config-standard": "^6.2.1",
    "eslint-plugin-promise": "^3.4.2",
    "eslint-plugin-standard": "^2.0.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0",
    "proxyquire": "^1.7.11",
    "sinon": "^1.17.7"
  },
  "dependencies": {
    "express": "^4.14.1"
  }
}
